// v14 rewards — intentionally empty. Present so the Function build does not
// inherit the Remix app's vite.config.ts from the repo root (same trick as
// Shopify's JS discount function template).
export default {};
