// v32 volume pricing — intentionally empty. Present so the Function build
// does not inherit the Remix app's vite.config.ts from the repo root (the
// cellexia-rewards / Shopify JS discount template trick).
export default {};
